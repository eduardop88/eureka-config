server:
  port: 8081
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eduardop88/application-config.git
          cloneOnStart: true
management:
  security:
    enabled:false
